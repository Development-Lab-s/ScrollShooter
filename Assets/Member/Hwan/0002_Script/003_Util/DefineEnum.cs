using UnityEngine;

public enum UIType
{
    None,
    SettingUI,
    GameOverUI
}

public enum InteractiveType
{
    None,
    Middle,
    Forward,
    Back,
    Left,
    Right,
    Scroll
}

public enum SettingType
{
    BGMSlider,
    SFXSlider,
    SensitivitySlider
}

public enum StartEnd
{
    Start,
    End
}